import React, { FC } from 'react';
import './styles.css';
import Navbar from '../../components/Navbar';

type Props = {};

const Landing: FC<Props> = (props: Props) => {
  return <Navbar />;
};

export { Landing };
