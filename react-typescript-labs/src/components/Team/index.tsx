import React, { FC } from 'react';
import { TeamContainer } from './TeamElements';

type Props = {};

const Team: FC<Props> = (props: Props) => {
  return (
    <>
      <TeamContainer id="team">hello</TeamContainer>
    </>
  );
};

export default Team;
