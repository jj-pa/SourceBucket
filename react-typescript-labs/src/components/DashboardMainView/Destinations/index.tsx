import React, { FC } from 'react';

type Props = {};

const Destinations: FC<Props> = (props: Props) => {
  return (
    <>
      <h2>components/DashboardMainView/Destinations</h2>
    </>
  );
};

export default Destinations;
