import React, { FC } from 'react';

type Props = {};

const About: FC<Props> = (props: Props) => {
  return (
    <>
      <h2>components/DashboardMainView/About</h2>
    </>
  );
};

export default About;
