import React, { FC } from 'react';

type Props = {};

const Contacts: FC<Props> = (props: Props) => {
  return (
    <>
      <h2>components/DashboardMainView/Contents</h2>
    </>
  );
};

export default Contacts;
