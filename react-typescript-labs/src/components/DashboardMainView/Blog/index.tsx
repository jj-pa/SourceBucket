import React, { FC } from 'react';

type Props = {};

const Blog: FC<Props> = (props: Props) => {
  return (
    <>
      <h2>components/DashboardMainView/Blog</h2>
    </>
  );
};

export default Blog;
