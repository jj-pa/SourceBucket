import React, { FC } from 'react';
import { SidebarContainer } from './DashboardSidebarElements';

type Props = {};

const DashboardSidebar: FC<Props> = (props: Props) => {
  return <SidebarContainer></SidebarContainer>;
};

export { DashboardSidebar };
